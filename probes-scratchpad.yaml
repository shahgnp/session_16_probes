apiVersion: v1
kind: Pod
metadata:
  name: probe-demo
  labels:
    app: probe-demo
spec:
  containers:
  - name: nginx
    image: nginx:alpine
    # Create a file /tmp/healthy on startup
    command: ["/bin/sh", "-c", "touch /tmp/liveness /tmp/readiness; sleep 3600"]
